# DOC-RECONCILIATION.md

This file was generated by the `/doc-truth-up` documentation-reconciliation pass. That pass treats
the code as read-only ground truth and edits only documentation so it reflects the repo's actual
current state. No code was changed, no builds were run, and no tests were executed — all findings
are based on reading source files, manifests, and git history.

---

## Per-Claim Findings

### 1. What it is

**Status:** `consistent`
**Evidence:** `src-tauri/src/lib.rs` registers commands for project management, spec parsing, test
generation, test execution, and alignment reporting. `src-tauri/src/services/` contains full
implementations of each stage. The README description ("verifies your code actually implements what
your spec says it should … extract requirements, generate tests, run them, and get a coverage
report") accurately matches the implemented pipeline.

---

### 2. Current state (implemented features)

**Status:** `consistent`
**Evidence:** All seven features listed in the README are implemented and tested:

| README claim                                                 | Verified by                                                                                                              |
| ------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------ |
| Requirement extraction via `pulldown-cmark` AST              | `src-tauri/src/services/spec_parser.rs:1` — uses `pulldown_cmark::Parser` with event-based AST traversal                 |
| Codebase scanning (TS, JS, Python, Rust, Go, Java, Ruby, C#) | `src-tauri/src/services/codebase_scanner.rs:16-18` — `SOURCE_EXTENSIONS` list and per-language extractors                |
| Template test generation (Jest/PyTest, Arrange/Act/Assert)   | `src-tauri/src/services/template_generator.rs:4-67` — both `generate_jest_test` and `generate_pytest_test`               |
| LLM test generation (Claude API, optional)                   | `src-tauri/src/services/llm_generator.rs:29-79` — calls `api.anthropic.com/v1/messages`                                  |
| Real test execution (120s timeout, pipe-deadlock-safe)       | `src-tauri/src/services/test_runner.rs:7,17-65` — `TEST_TIMEOUT` constant, threaded stdout/stderr readers                |
| Alignment reports with 4 mismatch categories                 | `src-tauri/src/services/alignment.rs:36-97` — `no_test_generated`, `not_implemented`, `test_failing`, `partial_coverage` |
| Export (JSON, HTML, CSV)                                     | `src-tauri/src/commands/report.rs:54-121` — three format branches                                                        |

---

### 3. Stack

**Status:** `consistent`
**Evidence:**

| Layer               | Claimed                               | Verified by                                                              |
| ------------------- | ------------------------------------- | ------------------------------------------------------------------------ |
| Desktop shell       | Tauri 2                               | `src-tauri/tauri.conf.json:1` (`$schema: .../2`)                         |
| Frontend            | React, TypeScript, Tailwind CSS       | `package.json:50-60` (react, typescript, tailwindcss)                    |
| Requirement parsing | Rust + pulldown-cmark, AST-based      | `src-tauri/Cargo.toml:26`; `spec_parser.rs:1`                            |
| Test execution      | Rust subprocess runner (Jest, PyTest) | `src-tauri/src/services/test_runner.rs:68,93`                            |
| LLM integration     | Anthropic Claude API (optional)       | `src-tauri/src/services/llm_generator.rs:43`                             |
| Storage             | SQLite (local app data dir)           | `src-tauri/Cargo.toml:24` (rusqlite bundled); `lib.rs:22` (app_data_dir) |

---

### 4. How to run

**Status:** `drifted` → **fixed**

The project uses **pnpm** as its package manager (confirmed by `pnpm-lock.yaml` in the repo root,
`src-tauri/tauri.conf.json:7-8` using `pnpm dev`/`pnpm build` as dev/build hooks, and
`package.json:92-95` containing a pnpm configuration block). The README's Installation and Usage
sections referenced `npm`, not `pnpm`.

**Changes made to `README.md`:**

- Installation: `npm install` → `pnpm install`
- Usage: `npm run tauri dev` → `pnpm tauri dev`

---

### 5. Known risks (doc ↔ code contradictions)

**Status:** `consistent` — no "Known Risks" section in README to drift.

---

### 6. Next move (demonstrably incomplete items)

**Status:** `unverifiable` — README has no roadmap or next-step section. The codebase shows no
prominent TODOs or stub functions in the main pipeline. AGENTS.md contains a "Next Recommended Move"
inside a portfolio-system context block (see Contradictions section below), but this refers to
external portfolio tracking state that cannot be verified by reading the code.

---

## Contradictions for Manual Review

These are drift items found in files outside the editable scope (`README.md`, `CLAUDE.md`,
`AGENTS.md`, `DOC-RECONCILIATION.md`, `docs/`). A human should apply the one-line fix.

### `CHANGELOG.md:10` — version mismatch

**Problem:** `CHANGELOG.md` documents `## [1.0.0] - 2026-03-21` as the current release, but
`package.json:3` and `src-tauri/Cargo.toml:3` both declare `version = "0.1.0"`.

**One-line fix:** Either bump `package.json` and `Cargo.toml` to `1.0.0`, or change the CHANGELOG
header to `[0.1.0]` — whichever reflects the intended release version.

---

### `AGENTS.md` — Portfolio Context block is stale

**Problem:** The `<!-- portfolio-context:start -->` block (AGENTS.md:25–64) says the project has
"`boilerplate` context" and recommends promoting it "beyond minimum-viable by capturing a dedicated
handoff, roadmap, or discovery artifact." The codebase shows a fully implemented pipeline with
comprehensive tests across all modules — it is not boilerplate.

**Unverifiable dimension:** The label "boilerplate context" specifically describes the _portfolio
tracking system's_ record quality (not the code quality), which is an external system this pass
cannot read. The "Next Recommended Move" advice is therefore likely stale but cannot be corrected
without updating the portfolio system itself.

**Suggested human action:** Re-run the portfolio context regeneration tool or manually update the
block to reflect the project's actual implemented state.

---

## Footer

**Generated by:** `/doc-truth-up` documentation-reconciliation pass (read-only; no code changed,
no builds run)
**Timestamp:** 2026-06-02 19:34:34 PDT
**Branch:** `docs/truth-up-2026-06-02`
**HEAD reconciled against:** `92e230330cb04c2373465e65f597c5e3e312f967`
