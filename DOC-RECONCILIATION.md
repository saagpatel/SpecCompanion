# DOC-RECONCILIATION

This file was generated by the `/doc-truth-up` documentation-reconciliation pass,
which treats the code as read-only ground truth and edits only documentation so it
reflects the repo's actual current state. It changed no source code, config, or
tests, and ran no builds or test executions. It exists so any reviewer can audit
exactly what was verified, what was changed, and why.

---

## Per-Claim Findings

### 1 · What It Is

**Status:** `consistent`
**Evidence:** `src-tauri/src/lib.rs` registers the full Tauri command surface
(project, spec, test-gen, test-exec, report, git commands). `src-tauri/src/main.rs`
delegates to `spec_companion_lib::run()`. `tauri.conf.json` identifies the product
as "Spec Companion" (identifier `com.speccompanion.desktop`). The README description
("native desktop app that verifies your code actually implements what your spec says")
accurately matches the implemented pipeline.

---

### 2 · Current State

**Status:** `consistent`
**Evidence (verified by reading code):**

| Feature                                               | Evidence                                                                                                                                                                                       |
| ----------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Requirement extraction (pulldown-cmark AST)           | `src-tauri/src/services/spec_parser.rs:1-69` — full AST walk, no regex                                                                                                                         |
| Codebase scanning (TS/JS/Python/Rust/Go/Java/Ruby/C#) | `src-tauri/src/services/codebase_scanner.rs:16-18` — `SOURCE_EXTENSIONS` array; all 8 languages have dedicated extractors                                                                      |
| Template test generation (Jest + PyTest)              | `src-tauri/src/services/template_generator.rs:4-68` — `generate_jest_test` and `generate_pytest_test` both implemented                                                                         |
| LLM test generation (Claude API, optional)            | `src-tauri/src/services/llm_generator.rs:29-80` — POST to `api.anthropic.com/v1/messages`, 60s timeout, api key required                                                                       |
| Real test execution                                   | `src-tauri/src/services/test_runner.rs:7` — `TEST_TIMEOUT = 120s`; `run_jest_test` spawns `npx jest`, `run_pytest_test` spawns `python -m pytest`; pipe deadlock prevention via reader threads |
| Alignment reports + mismatch categories               | `src-tauri/src/services/alignment.rs:36-97` — four types: `no_test_generated`, `not_implemented`, `partial_coverage`, `test_failing`                                                           |
| Export (JSON, HTML, CSV)                              | `src-tauri/src/commands/report.rs:54` — `matches!(format, "json" \| "html" \| "csv")`                                                                                                          |

All features claimed by the README are substantively implemented with tests
(`src-tauri/src/services/codebase_scanner.rs:342-688`,
`src-tauri/src/services/alignment.rs:126-277`,
`src-tauri/src/lib.rs:76-175`).

---

### 3 · Stack

**Status:** `consistent`
**Evidence:** `src-tauri/Cargo.toml` lists `tauri = "2"`, `rusqlite`, `pulldown-cmark`,
`reqwest`, `tokio`. `package.json` lists `react ^19`, `typescript ~5.8`, `tailwindcss ^4`,
`@tauri-apps/api ^2`. README tech stack table matches exactly.

---

### 4 · How to Run

**Status:** `drifted` → **fixed**

The README (and the AGENTS.md "How To Run" block) documented `npm install` and
`npm run tauri dev`. The project is a pnpm workspace: `package.json` contains a
`"pnpm": { "onlyBuiltDependencies": [...] }` field, all CI/test scripts reference
`pnpm` (e.g. `"test": "pnpm ui:gate"`), and `pnpm-workspace.yaml` was added in
commit `2a78cd0`. Using bare `npm install` on a pnpm workspace risks installing
with the wrong lockfile semantics.

**Changes made:**

| File                           | Before                              | After                             |
| ------------------------------ | ----------------------------------- | --------------------------------- |
| `README.md` Installation block | `npm install`                       | `pnpm install`                    |
| `README.md` Usage block        | `npm run tauri dev`                 | `pnpm tauri dev`                  |
| `AGENTS.md` How To Run block   | `npm install` / `npm run tauri dev` | `pnpm install` / `pnpm tauri dev` |

---

### 5 · Known Risks

**Status:** `unverifiable`
**Reason:** The AGENTS.md portfolio context notes "This repo only has minimum-viable
recovery context today; deeper handoff details may still live in the README." This is
an editorial note about documentation completeness at the time of writing, not a
verifiable claim about the current code state — no code reading can confirm or deny it.
Left unchanged.

---

### 6 · Next Move (Roadmap / Incomplete Work)

**Status:** `unverifiable`
**Reason:** No `ROADMAP.md`, `STATUS.md`, or `HANDOFF.md` exists. `CHANGELOG.md`
records a single `[1.0.0] - 2026-03-21` release with the full feature set marked
"Added". No TODOs or stubs were found in the service layer. The project appears
feature-complete relative to its documented scope; any forward-looking next steps
are not encoded in the code and cannot be verified by reading it.

---

## Contradictions for Manual Review

None. All contradictions found were in `README.md` and `AGENTS.md`, both of which
are within the allowed edit set and have been fixed above.

---

## Footer

**Generated:** 2026-05-30 21:09:05 PDT  
**Branch:** `docs/truth-up-2026-05-30`  
**HEAD sha reconciled against:** `92a2d0af6814b971104114624fcc5a3b18106ffc`
